"""
Standards & Interoperability — export SSV/runs to open scientific formats.

Submodules:
  rocrate      — SSV → RO-Crate v2 (JSON-LD, W3C PROV mapped)
  prov         — SSV + run → W3C PROV-O graph (PROV-JSON, PROV-N)
  openlineage  — run events → OpenLineage RunEvent format
  cwl          — pipeline → CWL v1.2 Workflow YAML
  output_formats — result → Parquet / Zarr archive
"""
